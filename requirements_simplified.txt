# Simplified AVHuBERT Requirements
# Core dependencies for the simplified implementation without fairseq

# PyTorch (core framework)
torch>=1.8.0
torchvision>=0.9.0

# Audio processing
python-speech-features==0.6
librosa>=0.8.0

# Video processing  
opencv-python>=4.5.0

# Scientific computing
numpy>=1.19.0
scipy>=1.5.0

# Optional: for better performance
# numba>=0.53.0  # accelerates librosa

# Development and testing (optional)
# pytest>=6.0.0
# pytest-cov>=2.10.0

# Optional: for additional audio formats
# soundfile>=0.10.0
# audioread>=2.1.0

# Optional: for tokenization (if extending for text output)
# sentencepiece>=0.1.96

# Optional: for distance metrics
# editdistance>=0.6.0